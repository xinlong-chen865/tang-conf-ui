---
import { Picture } from "astro:assets";
import heroImage from "assets/hero.png";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="Astronaut in the air"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
    />
  </div>
  <div>
    <h1
      class="text-4xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      一站式配置管理平台
    </h1>
    <h3 class="text-lg mt-4 mb-2 max-w-xl">解决痛点：</h3>
    <p class="text-lg text-slate-600 max-w-xl">
      统一各个不同系统的配置管理和使用方式
    </p>
    <p class="text-lg text-slate-600 max-w-xl">
      规范化配置变更操作流程，减少配置变更引发错误
    </p>
    <h3 class="text-lg mt-4 mb-2 max-w-xl">特点：</h3>
    <p class="text-lg text-slate-600 max-w-xl">
      前端友好：提供前端工程师，专业化node.js客户端。
    </p>
    <p class="text-lg text-slate-600 max-w-xl">
      易用：不同环境的配置可以统一配置。
    </p>
    <p class="text-lg text-slate-600 max-w-xl">
      权限管理：支持按不同层次的权限控制。
    </p>
    <h3 class="text-lg mt-4 mb-2 max-w-xl">注意：</h3>
    <p class="text-lg text-slate-600 max-w-xl">
      Tangconf的定位是服务配置，不能作为数据库使用
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="#"
        href="https://github.com/surjithctly/astroship"
        target="_blank"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:bxl-github" />
        GitHub Repo
      </Link>
    </div>
  </div>
</main>
